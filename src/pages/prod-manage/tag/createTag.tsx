import React, { memo } from 'react'
                
interface IProps {
 
}

const CreateTag: React.FC<IProps> = (props) => {
  return (
    <div>
          
    </div>
  )
}

export default memo(CreateTag)