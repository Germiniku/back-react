import React, { memo } from 'react'
                
interface IProps {
 
}

const UserProfile: React.FC<IProps> = (props) => {
  return (
    <div>
          
    </div>
  )
}

export default memo(UserProfile)